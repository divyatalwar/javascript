function user(name,age) {
  this.name = name;
  this.age = age;
}
user.prototype.compare=function(u2)
{
  if(this.age == u2.age)
    alert(" both are of same age");
  else {
    if(this.age > u2.age)
      alert(user1.name + " is older than " + user2.name);
    else
      alert(user2.name + " is older than " + user1.name);
  }
};
var user1 = new user("john",20);
var user2 = new user("mary",40);
user1.compare(user2);
